[project]
name = "waterloo-battle-site"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dan", email = "dan__init__@outlook.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
waterloo-battle-site = "waterloo_battle_site:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
